generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Business {
  id        Int      @id @default(autoincrement())
  name      String
  category  String
  location  String
  createdAt DateTime @default(now())
}